# Basic server example

This example starts a local PocketEdge HTTP/WebSocket host, exposes a public
health-style route, and issues a short-lived staff pairing token.

Run it from the package root with:

```bash
dart run example/basic_server/main.dart
```
